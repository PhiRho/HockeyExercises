[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]
packages = ["src"]
[project]
name = "hockey-exercise"
version = "2025.01.23"
dependencies = [
    "fastapi[standard]",
]
